<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.mapper.ContractMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.crm.entity.Contract">
        <id column="id" property="id" />
        <result column="number" property="number" />
        <result column="name" property="name" />
        <result column="amount" property="amount" />
        <result column="received_amount" property="receivedAmount" />
        <result column="sign_time" property="signTime" />
        <result column="customer_id" property="customerId" />
        <result column="opportunity_id" property="opportunityId" />
        <result column="status" property="status" />
        <result column="remark" property="remark" />
        <result column="delete_flag" property="deleteFlag" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="creater_id" property="createrId" />
        <result column="owner_id" property="ownerId" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
    </resultMap>


    <select id="getContractTrend" resultType="com.crm.vo.ContractTrendVO">
        SELECT
        COUNT(*) AS count,
        <choose>
            <when test="query.timeType == 'day'">DATE_FORMAT(sign_time, '%H') AS time</when>
            <when test="query.timeType == 'week'">DATE_FORMAT(sign_time, '%Y-%u') AS time</when>
            <when test="query.timeType == 'month'">DATE_FORMAT(sign_time, '%Y-%m') AS time</when>
            <otherwise>DATE_FORMAT(sign_time, '%Y') AS time</otherwise>
        </choose>
        FROM t_contract
        WHERE delete_flag = 0
        <if test="query.timeRange != null and query.timeRange.size() > 0">
            AND sign_time BETWEEN #{query.timeRange[0]} AND #{query.timeRange[1]}
        </if>
        GROUP BY time
        ORDER BY time
    </select>

    <select id="getContractStatusPie" resultType="com.crm.vo.ContractTrendVO">
        SELECT
            COUNT(*) AS count,
        CASE
            WHEN status = 0 THEN '初始化'
            WHEN status = 1 THEN '审核通过'
            WHEN status = 2 THEN '审核未通过'
            ELSE '其他'
        END AS name
        FROM t_contract
        WHERE delete_flag = 0
        GROUP BY status
    </select>

    <select id="getTodayNewCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_contract
        WHERE delete_flag = 0
          AND DATE(create_time) = CURDATE()
    </select>
</mapper>
